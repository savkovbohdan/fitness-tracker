# 🚀 Развертывание Фитнес-Трекера на Ubuntu

## 📋 Подготовка

1. **Подключись к серверу:**
   ```bash
   ssh root@178.212.12.73
   password: F9xdWa05tC
   ```

2. **Скопируй скрипт на сервер:**
   ```bash
   # Создай файл deploy_ubuntu.sh на сервере
   nano deploy_ubuntu.sh
   # Вставь содержимое скрипта и сохрани
   ```

## 🚀 Автоматическое развертывание

1. **Сделай скрипт исполняемым:**
   ```bash
   chmod +x deploy_ubuntu.sh
   ```

2. **Запусти развертывание:**
   ```bash
   ./deploy_ubuntu.sh
   ```

## 📦 Что делает скрипт

### ✅ Системные компоненты:
- Обновление Ubuntu (`apt update && apt upgrade`)
- Установка Node.js 18.x
- Установка PM2 (менеджер процессов)
- Установка Nginx (reverse proxy)
- Настройка файрвола

### 🏗️ Приложение:
- Создание директории `/var/www/fitness-tracker`
- Установка Node.js зависимостей
- Создание SQLite базы данных
- Добавление 10 базовых упражнений
- Настройка переменных окружения

### 🌐 Веб-сервер:
- Конфигурация Nginx reverse proxy
- Проксирование с порта 80 на 5001
- Автозапуск сервисов

## 🌐 Доступ к приложению

После развертывания приложение будет доступно:

- **Основной адрес:** `http://178.212.12.73`
- **Прямой порт:** `http://178.212.12.73:5001`
- **API эндпоинты:** `http://178.212.12.73:5001/api`

## 🔧 Управление приложением

### PM2 команды:
```bash
# Статус процессов
pm2 status

# Логи приложения
pm2 logs fitness-tracker

# Перезапуск
pm2 restart fitness-tracker

# Остановка
pm2 stop fitness-tracker

# Мониторинг
pm2 monit
```

### Nginx команды:
```bash
# Статус
systemctl status nginx

# Перезапуск
systemctl restart nginx

# Тест конфигурации
nginx -t

# Логи
tail -f /var/log/nginx/access.log
tail -f /var/log/nginx/error.log
```

## 📁 Структура файлов

```
/var/www/fitness-tracker/
├── server.js                 # Express.js бэкенд
├── package.json             # Зависимости Node.js
├── ecosystem.config.js      # PM2 конфигурация
├── .env                    # Переменные окружения
├── fitness_tracker.db      # SQLite база данных
└── public/
    └── index.html         # Frontend
```

## 🔗 API Эндпоинты

### Пользователи:
- `POST /api/users` - создание пользователя
- `GET /api/users/:telegram_id` - получение пользователя

### Упражнения:
- `GET /api/exercises` - все упражнения
- `POST /api/exercises` - новое упражнение

### Тренировки:
- `GET /api/workout-logs/:user_id` - история
- `POST /api/workout-logs` - запись тренировки

### Статистика:
- `GET /api/stats/:user_id` - статистика

## 🛠️ Ручное развертывание (если автоматическое не сработало)

### 1. Установка Node.js:
```bash
curl -fsSL https://deb.nodesource.com/setup_18.x | bash -
apt-get install -y nodejs
```

### 2. Установка PM2:
```bash
npm install -g pm2
```

### 3. Создание директории:
```bash
mkdir -p /var/www/fitness-tracker
cd /var/www/fitness-tracker
```

### 4. Загрузка файлов:
```bash
# Скопируй файлы приложения в /var/www/fitness-tracker
# Используй scp или git clone
```

### 5. Установка зависимостей:
```bash
npm install
```

### 6. Запуск через PM2:
```bash
pm2 start server.js --name "fitness-tracker"
pm2 startup
pm2 save
```

## 🔒 Безопасность

### Файрвол:
```bash
# Открыть порт 5001
ufw allow 5001/tcp

# Открыть порт 22 (SSH)
ufw allow 22/tcp

# Включить файрвол
ufw enable
```

### SSL (опционально):
```bash
# Установка Certbot
apt install -y certbot python3-certbot-nginx

# Получение SSL сертификата
certbot --nginx -d your-domain.com

# Автопродление
echo "0 12 * * * /usr/bin/certbot renew --quiet" | crontab -
```

## 📊 Мониторинг

### Системные ресурсы:
```bash
# CPU и память
htop
free -h

# Дисковое пространство
df -h

# Нагрузка на сеть
iftop
```

### Логи приложения:
```bash
# PM2 логи
pm2 logs fitness-tracker --lines 100

# Nginx логи
tail -f /var/log/nginx/access.log
```

## 🚨 Поиск проблем

### Приложение не запускается:
```bash
# Проверь порт
netstat -tlnp | grep :5001

# Проверь процесс
ps aux | grep node

# Логи ошибок
pm2 logs fitness-tracker --err
```

### Nginx не работает:
```bash
# Проверь конфигурацию
nginx -t

# Проверь статус
systemctl status nginx

# Логи ошибок
tail -f /var/log/nginx/error.log
```

### База данных:
```bash
# Проверь файл БД
ls -la /var/www/fitness-tracker/fitness_tracker.db

# Права доступа
ls -la /var/www/fitness-tracker/
```

## 🎯 Telegram Bot интеграция

1. **Создай бота** через @BotFather
2. **Получи токен** и добавь в `.env`
3. **Обнови WebApp URL** в боте:
   ```
   https://178.212.12.73
   ```

## 📞 Поддержка

### Полезные команды:
```bash
# Перезагрузить сервер
reboot

# Проверить время работы
uptime

# Информация о системе
uname -a
```

---

**🎉 Готово! После выполнения скрипта твой Фитнес-Трекер будет доступен по адресу `http://178.212.12.73`**
