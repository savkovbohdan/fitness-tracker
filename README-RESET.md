# Database Reset Guide

## ⚠️ Внимание!
Этот скрипт **полностью удалит** все данные из базы данных fitness_tracker, включая:
- Все упражнения (базовые и пользовательские)
- Все логи тренировок
- Всех пользователей
- Все загруженные фотографии

## 🗑️ Как сбросить базу данных

### Вариант 1: Автоматический скрипт (рекомендуется)

На сервере выполните:

```bash
cd /var/www/fitness-tracker
sudo ./reset-database.sh
```

### Вариант 2: Ручной сброс

```bash
# 1. Остановить приложение
pm2 stop fitness-tracker
pm2 delete fitness-tracker

# 2. Удалить базу данных
sudo -u postgres psql -c "DROP DATABASE IF EXISTS fitness_tracker;"

# 3. Создать новую базу данных
sudo -u postgres psql -c "CREATE DATABASE fitness_tracker;"

# 4. Создать пользователя
sudo -u postgres psql -c "CREATE USER fitness_user WITH PASSWORD 'postgres123';"

# 5. Выдать права
sudo -u postgres psql -c "GRANT ALL PRIVILEGES ON DATABASE fitness_tracker TO fitness_user;"

# 6. Запустить приложение
cd /var/www/fitness-tracker
pm2 start server.js --name fitness-tracker
```

### Вариант 3: Через SSH

```bash
ssh root@178.212.12.73
cd /var/www/fitness-tracker
sudo ./reset-database.sh
```

## 🔄 Что делает скрипт

1. **Останавливает приложение** (PM2)
2. **Удаляет базу данных** `fitness_tracker`
3. **Удаляет пользователя** `fitness_user`
4. **Создает новую базу данных** `fitness_tracker`
5. **Создает нового пользователя** `fitness_user`
6. **Выдает права** на базу данных
7. **Устанавливает пароль** для postgres
8. **Запускает приложение**
9. **Проверяет работу** API
10. **Инициализирует базу данных** с базовыми упражнениями

## 📊 Результат после сброса

- ✅ **Чистая база данных** без старых данных
- ✅ **10 базовых упражнений** с GIF фотографиями
- ✅ **Тестовый пользователь** (ID: 12345)
- ✅ **Работающее приложение** на порту 5001
- ✅ **API готов** к использованию

## 🔐 Данные после сброса

### Базовые упражнения (создаются автоматически):
1. Жим лежа (грудь)
2. Приседания со штангой (ноги)
3. Становая тяга (спина)
4. Подтягивания (спина)
5. Армейский жим (плечи)
6. Бицепс со штангой (руки)
7. Трицепс на блоке (руки)
8. Сгибания ног (ноги)
9. Гиперэкстензия (спина)
10. Скручивания (пресс)

### Пользователи:
- Тестовый пользователь: `testuser` (ID: 12345)

## ⚡ Быстрая проверка после сброса

```bash
# Проверить API
curl http://178.212.12.73/api/health

# Проверить упражнения
curl http://178.212.12.73/api/exercises

# Проверить количество упражнений
curl -s http://178.212.12.73/api/exercises | jq '. | length'
```

## 🚨 Предупреждения

- **Все данные будут безвозвратно удалены**
- **Пользовательские упражнения** нужно будет создавать заново
- **История тренировок** будет очищена
- **Загруженные фотографии** будут удалены

## 📝 Когда использовать сброс

- **При проблемах с базой данных**
- **При ошибках миграции**
- **При тестировании нового функционала**
- **При повреждении данных**
- **При изменении структуры таблиц**

## 🆘 Восстановление данных

После сброса нужно будет:
1. Создать пользовательские упражнения заново
2. Загрузить фотографии заново
3. Начать новые тренировки

## 📞 Поддержка

Если возникнут проблемы:
1. Проверьте права доступа к PostgreSQL
2. Убедитесь что postgres пользователь существует
3. Проверьте логи PM2: `pm2 logs fitness-tracker`
4. Проверьте логи приложения: `tail -f /var/www/fitness-tracker/logs/*.log`
