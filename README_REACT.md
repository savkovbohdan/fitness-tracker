# 🏋️‍♂️ Фитнес-Трекер React App

Современное React приложение с SQLite3 бэкендом для учета тренировок.

## ✨ Функционал

### 🎨 React Frontend
- **Современный интерфейс** на React 18
- **Telegram Web App** интеграция
- **Адаптивный дизайн** с CSS-in-JS
- **Плавные анимации** и переходы
- **Тактильная обратная связь** через вибрацию

### 🗄️ SQLite3 Backend
- **RESTful API** на Express.js
- **Полная база данных** с пользователями, упражнениями, тренировками
- **Асинхронные операции** с промисами
- **Автоматическая инициализация** схемы БД

### 💪 Тренировки
- **Пошаговый ввод** подходов с весом и повторениями
- **Гибкое количество** подходов
- **Собственный вес** или дополнительный вес
- **Мгновенное сохранение** в базу данных

### 📊 Учет и статистика
- **История тренировок** с детализацией
- **Статистика прогресса** по упражнениям
- **Максимальные веса** и повторения
- **Общее количество** подходов и повторений

### ➕ Персонализация
- **Добавление упражнений** с категориями
- **6 категорий** (ноги, грудь, спина, руки, плечи, пресс)
- **Фотографии упражнений** (опционально)
- **10 базовых упражнений** предустановлено

## 🚀 Установка и запуск

### 1. Установка зависимостей
```bash
npm install --legacy-peer-deps
```

### 2. Запуск приложения
```bash
# Запуск и фронтенда и бэкенда одновременно
npm start

# Или отдельно:
npm run server  # Бэкенд на порту 5000
npm run client  # Фронтенд на порту 3000
```

### 3. Доступ к приложению
- **Frontend:** http://localhost:3000
- **API:** http://localhost:5000/api
- **База данных:** `fitness_tracker.db` (создается автоматически)

## 🗂️ Структура проекта

```
fitness-tracker-react/
├── package.json              # Зависимости и скрипты
├── server.js                # Express.js бэкенд
├── fitness_tracker.db        # SQLite3 база данных
├── public/                  # Статические файлы
│   └── index.html          # HTML с Telegram Web App
├── src/                     # React исходники
│   ├── components/          # Компоненты экранов
│   │   ├── MainScreen.js
│   │   ├── ExercisesScreen.js
│   │   ├── WorkoutScreen.js
│   │   ├── HistoryScreen.js
│   │   ├── StatsScreen.js
│   │   └── AddExerciseScreen.js
│   ├── services/           # API сервисы
│   │   └── api.js
│   ├── App.js             # Главный компонент
│   ├── App.css            # Стили
│   └── index.js           # Точка входа
└── README_REACT.md        # Этот файл
```

## 🔧 Технологический стек

### Frontend:
- **React 18** - современный UI фреймворк
- **CSS-in-JS** - стили внутри компонентов
- **Telegram Web App API** - интеграция с Telegram
- **Axios** - HTTP клиент для API запросов

### Backend:
- **Node.js** - JavaScript runtime
- **Express.js** - веб-фреймворк
- **SQLite3** - легковесная база данных
- **CORS** - поддержка кросс-доменных запросов

### База данных:
```sql
-- Пользователи
CREATE TABLE users (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    telegram_id INTEGER UNIQUE,
    username TEXT,
    first_name TEXT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- Упражнения
CREATE TABLE exercises (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL,
    category TEXT NOT NULL,
    photo_path TEXT,
    is_custom INTEGER DEFAULT 0,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- Тренировочные записи
CREATE TABLE workout_logs (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    user_id INTEGER NOT NULL,
    exercise_id INTEGER NOT NULL,
    set_number INTEGER NOT NULL,
    weight REAL NOT NULL,
    reps INTEGER NOT NULL,
    date TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (user_id) REFERENCES users (id),
    FOREIGN KEY (exercise_id) REFERENCES exercises (id)
);
```

## 📱 API Эндпоинты

### Пользователи:
- `POST /api/users` - создать пользователя
- `GET /api/users/:telegram_id` - получить пользователя

### Упражнения:
- `GET /api/exercises` - получить все упражнения
- `POST /api/exercises` - создать упражнение

### Тренировки:
- `GET /api/workout-logs/:user_id` - история тренировок
- `POST /api/workout-logs` - записать тренировку

### Статистика:
- `GET /api/stats/:user_id` - статистика по упражнениям

## 🎨 Особенности интерфейса

### Дизайн:
- **Зеленая цветовая схема** с градиентами
- **Стекломорфизм** и полупрозрачность
- **Адаптивная верстка** для всех экранов
- **Плавные анимации** и микро-взаимодействия

### UX:
- **Тактильная обратная связь** через Telegram Web App
- **Мгновенные переходы** между экранами
- **Интуитивная навигация** с кнопками "назад"
- **Визуальная обратная связь** при действиях

## 🚀 Развертывание

### Production:
```bash
# Сборка фронтенда
npm run build

# Запуск бэкенда с production сборкой
NODE_ENV=production npm start
```

### Docker:
```dockerfile
FROM node:18-alpine
WORKDIR /app
COPY package*.json ./
RUN npm ci --only=production
COPY . .
RUN npm run build
EXPOSE 5000
CMD ["npm", "start"]
```

## 🔄 Интеграция с Telegram

### Mini App:
1. **Размести приложение** на веб-сервере
2. **Создай Telegram бота** через @BotFather
3. **Добавь WebApp кнопку** в бота
4. **Передай данные пользователя** через initData

### Пример интеграции:
```javascript
// Инициализация Telegram Web App
const tg = window.Telegram.WebApp;
tg.expand();
tg.ready();

// Получение данных пользователя
const user = tg.initDataUnsafe?.user;
```

## 📈 Возможности для развития

- [ ] **Графики прогресса** с Chart.js
- [ ] **Тренировочные программы** и планы
- [ ] **Экспорт данных** в CSV/JSON
- [ ] **Социальные функции** - друзья, челленджи
- [ ] **Push-уведомления** о тренировках
- [ ] **Мультимедиа** - видео упражнений
- [ ] **AI-рекомендации** тренировок

## 🛠️ Разработка

### Добавление нового экрана:
```javascript
// src/components/NewScreen.js
import React from 'react';

const NewScreen = ({ user, onNavigate }) => {
  return (
    <div className="slide-up">
      <div className="header">
        <h1>Новый экран</h1>
      </div>
      {/* Контент */}
    </div>
  );
};

export default NewScreen;
```

### Добавление нового API эндпоинта:
```javascript
// server.js
app.get('/api/new-endpoint', (req, res) => {
  // Логика эндпоинта
  res.json(data);
});
```

## 📄 Лицензия

MIT License

## 👨‍💻 Автор

Создано с использованием современных веб-технологий

---

**🚀 Начни свой фитнес-путь с React приложением уже сегодня!**
